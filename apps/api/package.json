{
  "name": "@docpilot/api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/server.js",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc --build",
    "start": "node dist/server.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@docpilot/shared": "*"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}
